import React from "react";
import { View, Text } from "react-native";

export default class CacheAndLoadingVideoScreen extends React.Component {
  render() {
    return (
      <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
        <Text>Cache And Loading Screen</Text>
      </View>
    );
  }
}
